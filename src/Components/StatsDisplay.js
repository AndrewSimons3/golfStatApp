import classes from './StatsDisplay.module.css';

const StatsDisplay = () => {
  return (
    <div className={classes.statsDisplayWrapper}>
      <div className={classes.statsDisplay}>
        DISPLAY STATS
      </div>
    </div>
  )
};

export default StatsDisplay;